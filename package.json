{
  "name": "cors_middleware",
  "version": "1.0.0",
  "description": "Middleware to avoid CORS issues",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Menilik Belay Woldeyes",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
